version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/auth_info_baileys:/app/auth_info_baileys
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
